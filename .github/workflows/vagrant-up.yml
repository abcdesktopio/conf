name: vagrant-up

on: [push]

jobs:
  vagrant-up:
    # runs-on: self-hosted
    runs-on: macos-10.15

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run script file
      run: |
         ls -la 
      shell: bash

    - name: Run vagrant up
      run: vagrant up
